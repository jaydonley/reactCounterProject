import React from 'react'

const Reset = (props) => {
    const { Reset } = props

    return (
        <div>
            <button onClick = {Reset}>Reset to Zero</button>
        </div>
    )
}

export default Reset