import React from 'react'

const Reset = (props) => {
    const { Reset } = props

    return (
        <div>
            <button onClick = {Reset}>Let's Start Over!</button>
        </div>
    )
}

export default Reset